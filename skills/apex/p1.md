---
name: p1
description: Path 1 (medium / trivial / zero-layer) chain. Same chain serves main mode and teammate mode; --teammate flag trims the chain. Owns p1.0 init, dispatches p1.1 implement, p1.1b polish, p1.2 verify+fix, p1.3 tail (learn/docs/git), p1.4 self-reflect, p1.5 cleanup, p1.6 inline summary.
---

# p1 (Path 1 chain)

Spec: `apex-core.md` "Path 1 (p1)" + "Teammate mode" subsection | `apex-core-overview.md` "Path 1".

## Invocation

| Caller | Mode | Flag | Notes |
|--------|------|------|-------|
| step 5 trivial branch (`trivial.md`) | main | (none) | preflight absent |
| step 6.a zero-layer proceed (`scout1.md`) | main | (none) | preflight absent |
| step 9 medium dispatch (`scripts/decide-path.sh`) | main | (none) | preflight present + valid |
| `teammates.md` at p2.1 | teammate | `--teammate` | preflight present (already validated by main); MUST receive flag |

Parse args at entry:

```
MODE=main
for arg in "$@"; do
  [[ "$arg" == "--teammate" ]] && MODE=teammate
done
```

## p1.0 Initialisation

### Step 1: Validate preflight artifact

Read `.claude-tmp/scout/preflight-{session}.json` and decide branch:

```
preflight=".claude-tmp/scout/preflight-{session}.json"
if [[ ! -f "$preflight" ]]; then
  branch=absent
elif PYTHONPATH="$HOME/.claude/skills/apex/scripts" python3 -c "
import sys
from _validate import consumer_load
sys.exit(0 if consumer_load('$preflight', 'preflight') is not None else 1)
" 2>/dev/null; then
  branch=valid
else
  branch=invalid
fi
```

| Branch | Action |
|--------|--------|
| `absent` | trivial / zero-layer - `implement.md` proceeds with NO findings consultation |
| `valid` | medium - `implement.md` consults `screened-{session}.json` before dispatch |
| `invalid` | HARD ABORT with explicit error to user (downstream-pipeline failure signal); run `scripts/session-end-hook.sh {session}` inline; do NOT silently fall through |

### Step 2: Mode-specific setup

**Main mode (`MODE=main`):**

```
# 2a. Capture working-tree baseline (consumed by p1.1b polish, p1.3 tail, p1.4 reflector)
bash $HOME/.claude/skills/apex/scripts/apex-baseline.sh {session}

# 2b. Concurrent-apex conflict check; stdout = "<file> -> <scope-basename>" per overlap
overlap=$(bash $HOME/.claude/skills/apex/scripts/apex-conflict-check.sh {session}) || conflict=1
```

On `conflict=1` (script exit 1; overlap list captured in `$overlap`):
- AskUserQuestion (header: "Concurrent apex"; options: `abort`, `proceed-anyway`; dismiss/cancel = abort)
- abort -> run `scripts/session-end-hook.sh {session}` inline; exit cleanly
- proceed-anyway -> pre-dirty files are processed normally per "process as normal file" rule (staged, polished, committed alongside apex-introduced edits)

**Teammate mode (`MODE=teammate`):**

Skip 2a + 2b (p2.0 owns baseline + conflict check; both shared by all teammates).

Write the scope-check pointer for the teammate's own Claude Code session:

```
pointer_dir=".claude-tmp/apex-active/{session}-scopes"
mkdir -p "$pointer_dir"
printf '%s\n' "$(realpath .claude-tmp/apex-active/{session}-{teammate-id}-scope.json)" \
  > "$pointer_dir/{teammate_cc_session_id}.txt"
```

The teammate's `cc_session_id` is the new Claude Code session id for the spawned `TeamCreate` teammate (distinct from the main / p2 session).

### Step 3: TaskCreate downstream chain

**Main mode:**

```
TaskCreate "p1.1 Implement"             - blockedBy [p1.0]   - implement.md
TaskCreate "p1.1b In-scope polish"      - blockedBy [p1.1]   - polish.md
TaskCreate "p1.2 Verify + fix"          - blockedBy [p1.1b]  - verify-fix.md --phase p1 (-> scripts/verify-build.sh + agents/executor.md)
TaskCreate "p1.3 Tail (learn/docs/git)" - blockedBy [p1.2]   - tail.md --phase p1 (-> scripts/detect-tail-mode.sh + agents/{learn,documentation,git}.md)
TaskCreate "p1.4 Self-reflect"          - blockedBy [p1.3]   - reflect.md --phase entryflow+p1 (-> scripts/reflect-traces.sh + agents/reflector.md)
TaskCreate "p1.5 Cleanup"               - blockedBy [p1.4]   - scripts/cleanup-session.sh
TaskCreate "p1.6 Inline summary"        - blockedBy [p1.5]   - summary.md --phase p1 (reads {session}-hypothesis.json)
```

**Teammate mode** (skips p1.2, p1.4, p1.5 - central p2.3 / p2.5 / p2.6 own these):

```
TaskCreate "p1.1 Implement"             - blockedBy [p1.0]   - implement.md
TaskCreate "p1.1b In-scope polish"      - blockedBy [p1.1]   - polish.md
TaskCreate "p1.3 Tail (docs only)"      - blockedBy [p1.1b]  - tail.md --phase teammate (-> agents/documentation.md only)
TaskCreate "p1.6 Inline summary"        - blockedBy [p1.3]   - summary.md --phase teammate (reads {session}-{teammate-id}-task.md)
```

Per-step skill files are listed in the TaskCreate templates above; canonical step contracts live in `apex-core.md` "Path 1".

Mid-abort cleanup: see `shared-guardrails.md` "Mid-/apex abort cleanup".

See `shared-guardrails.md` for safety paths, scope pointer rules, trace path schema, JSON Schema validation.
