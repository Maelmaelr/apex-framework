{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "baseline.schema.json",
  "title": "{session}-baseline.json (working-tree baseline at step 8.0)",
  "description": "Captured by apex-baseline.sh. head_sha is the git rev-parse HEAD value at step 8.0 init. Consumed by step 9 (polish), step 11 (tail diff), step 12 (git stage filter), and step 13 (reflector diff context).",
  "type": "object",
  "required": ["head_sha"],
  "properties": {
    "head_sha": {
      "type": "string",
      "pattern": "^[0-9a-f]{7,40}$",
      "description": "Lowercase hex git commit sha (7-40 chars) recorded at step 8.0. Consumers derive modified files via `git diff --name-only {head_sha}` (worktree working tree is clean at session mint by construction; no pre_dirty filter needed)."
    }
  },
  "additionalProperties": true
}
