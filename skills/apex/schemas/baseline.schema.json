{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "baseline.schema.json",
  "title": "{session}-baseline.json",
  "description": "Working-tree baseline captured at p1.0 (main) or p2.0. Single producer per session. Consumed by p1.1b polish, p1.3/p2.4 detect-tail-mode + tail agents, p1.4/p2.5 reflector. See apex-core.md p1.0 / p2.0.",
  "type": "object",
  "required": ["head_sha"],
  "properties": {
    "head_sha": { "type": "string", "pattern": "^[0-9a-f]{7,40}$", "description": "Output of git rev-parse HEAD at baseline-capture time" }
  }
}
