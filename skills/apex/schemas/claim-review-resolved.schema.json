{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "claim-review-resolved.schema.json",
  "title": "claim-review-resolved-{session}.json",
  "description": "Orchestrator's per-claim verdict from inline review. Consumed by verify-claims.sh --apply-resolved. See apex-core.md step 8.",
  "type": "array",
  "items": {
    "type": "object",
    "required": ["file", "action", "reason"],
    "properties": {
      "file": { "type": "string" },
      "action": { "enum": ["keep", "drop"] },
      "reason": { "type": "string" }
    }
  }
}
