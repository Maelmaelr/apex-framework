{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "verify-rerun.schema.json",
  "title": "{session}-verify-rerun.json",
  "description": "verify-claims.sh exit-2 cap counter. Dedicated file (not _meta) to survive aggregator rewrites of screened-{session}.json. Cap = 1. See apex-core.md step 8.",
  "type": "object",
  "required": ["count"],
  "properties": {
    "count": { "type": "integer", "minimum": 0, "maximum": 1 },
    "last_attempt_at": { "type": "string", "format": "date-time" }
  }
}
