# apex step 11 - Tail (full contract)

## Contract

**Tail** (foreground; Sonnet latest):
- **standard**: `agents/documentation.md` UNLESS `hypothesis.mode == "code-only-no-docs"` (mode-skip clause; doc agent ran despite mode flag, 4 doc files mutated); `agents/learn.md` ONLY when the **difficulty gate** holds (file `{session}-fix-attempts.json` exists AND its `count >= 1`). First-try-clean sessions skip `learn` entirely - the difficulty gate is the load-bearing volume control (without it `learn` ran on every standard session, producing ~hundreds of low-signal lines/day); the lessons file stays lean by construction. When both run, dispatch in parallel. **doc-inline marker**: when documentation.md is skipped because an in-scope doc file (`docs/**`, `*.md`, `README*`, `CLAUDE.md`) was already edited inline by an executor or by the orchestrator, append `doc-inline: <repo-relative-path>` to dispatch-summary `notes` (one per touched doc) so step-13 reflector distinguishes inline-doc-done from a skipped doc-agent step.
- **economy**: `agents/documentation.md` only (`learn` skipped); same `hypothesis.mode == "code-only-no-docs"` skip applies.
- Both spawn prompts MUST pass `project_root` (absolute session-worktree path) + `diff_anchor` + the session branch name, and mandate `cd "$project_root"` + `git branch --show-current` assert as the agent's FIRST action - tail subagents do not reliably inherit the worktree CWD, and a main-tree diff silently no-ops the agent (cluster: learn-agent-wrong-tree). Both read `git -C "$project_root" diff {diff_anchor}`. `learn` appends to `.claude-tmp/lessons-tmp.md` under `flock` (via `bash skills/apex/scripts/append-with-lock.sh`); the agent's own bar drops within-session trivia even when the gate opens. `learn` is project-specific (codebase patterns); the step-13 `reflector` is apex-specific (workflow / pipeline improvements).
