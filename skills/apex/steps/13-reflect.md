# apex step 13 - Self-reflect

Lazy-loaded contract for orchestrator step 13. Dispatched from `skills/apex/SKILL.md`
step 13; Read this file before executing the step so the rule is maximally recent
(B/R3 read-before-work). The item-3 step-read gate enforces the read once armed;
until then the dispatch is a soft convention. Full per-step contract (artifacts,
exit codes, abort paths): `apex-core.md` step 13.

## Contract

`bash skills/apex/scripts/reflect-traces.sh --session {session}` writes a heuristic block to `~/.claude/tmp/apex-workflow-improvements.md` under `flock` (via `bash skills/apex/scripts/append-with-lock.sh`). Then spawn `agents/reflector.md` (Sonnet, **background**, silent). **Pre-spawn hypothesis gate**: before spawning, verify `.claude-tmp/apex-active/{session}-hypothesis.json` exists on disk; if it is absent (orchestrator never wrote it pre-reflect), the spawn-prompt MUST carry an explicit `hypothesis_json: MISSING - reflect on traces + diff alone, no original_prompt/alternatives signal` line so the reflector degrades deliberately rather than silently emitting a weaker block (hypothesis.json absent from the active dir deprived the reflector of the original_prompt + alternatives signal with no fallback marker). The spawn-prompt MUST include `PROJECT_ROOT="$PWD"` (the orchestrator's absolute project root) so the reflector can pass `--apex-active-dir "${PROJECT_ROOT}/.claude-tmp/apex-active"` to cleanup-session.sh - background subagent CWD inheritance is unreliable and a bare relative resolution silently no-ops (left manifest + 4 siblings under `/Users/mael/Dev/flowctory/.claude-tmp/apex-active` because cleanup ran from the wrong CWD). Reflector reads traces in-place from `.claude-tmp/apex-active/{session}-traces/` (NO snapshot). Reflector owns post-reflect cleanup: as its final action it runs `bash skills/apex/scripts/cleanup-session.sh --session {session} --apex-active-dir "${PROJECT_ROOT}/.claude-tmp/apex-active"` (idempotent; worktree-only - forks on worktree state without any PID / cc_session_id guard). Reflector also runs **non-convergence detection**: appends `{ts, session, hash=sha1(normalized original_prompt), scope_count, touched_count, files_touched}` to `~/.claude/tmp/apex-prompt-history.log`; on hash collision with a prior session that touched a different file set, surfaces a `non-convergence:` line in `improvements:` for `/apex-improve` to consume. Backgrounded so the orchestrator releases the user immediately and proceeds to step 15. Step 15.0 owns the cd-back sweep (single canonical guard - the prior step-13 mid-paragraph cd-back drifted across long-context runs and was collapsed into 15.0).
